{
  "BANNER_TEXT": "Comment Replies - Last Hour",
  "FROM_NAME": "FastComments Robot",
  "SUBJECT": "FastComments - <%= abbreviateNumber(replyCount, 0, locale) %> New <% if(replyCount === 1) {%>Reply<% } else { %>Replies<% } %> to Your <% if(replyCount > 1) { %>Comments<% } else {%>Comment<% } %>",
  "VIEW_REPLIES": "View <%= abbreviateNumber(replyCount, 0, locale) %> <% if(replyCount === 1) {%>Reply<% } else { %>Replies<% } %>",
  "DISABLE_NOTIFICATIONS_FOR_COMMENT": "Disable Notifications For This Comment"
}