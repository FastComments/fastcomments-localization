{
    "INTRO": "嘿 <%= username %>，",
    "BANNER_TEXT": "新的投票回应",
    "FROM_NAME": "FastComments 机器人",
    "SUBJECT": "您的<% if(polls.length > 1) { %>投票<% } else { %>投票<% } %>收到了 <%= abbreviateNumber(newResponseCount, 0, locale) %> 个<% if(newResponseCount === 1) { %>新回应<% } else { %>新回应<% } %>",
    "NEW_RESPONSES_SUMMARY": "您的<% if(polls.length > 1) { %>投票<% } else { %>投票<% } %>收到了新的回应。以下是<% if(polls.length > 1) { %>目前的结果<% } else { %>目前的结果<% } %>。",
    "NEW_RESPONSES_LABEL": "新回应数：",
    "TOTAL_RESPONSES_LABEL": "总回应数：",
    "OPTION": "选项",
    "RESPONSES": "回应数",
    "SHARE": "占比",
    "VIEW_POLL": "查看投票",
    "DISABLE_NOTIFICATIONS_FOR_POLL": "禁用此投票的通知",
    "UNSUBSCRIBE_HERE": "退订"
}