{
    "INTRO": "Hej <%= username %>,",
    "BANNER_TEXT": "Nye stemmer",
    "FROM_NAME": "FastComments Robot",
    "SUBJECT": "<%= abbreviateNumber(newResponseCount, 0, locale) %> <% if(newResponseCount === 1) { %>Ny stemme<% } else { %>Nye stemmer<% } %> til <% if(polls.length > 1) { %>Dine afstemninger<% } else { %>Din afstemning<% } %>",
    "NEW_RESPONSES_SUMMARY": "<% if(polls.length > 1) { %>Dine afstemninger har<% } else { %>Din afstemning har<% } %> fået nye stemmer. Her er <% if(polls.length > 1) { %>de aktuelle resultater<% } else { %>det aktuelle resultat<% } %>.",
    "NEW_RESPONSES_LABEL": "Nye stemmer:",
    "TOTAL_RESPONSES_LABEL": "Stemmer i alt:",
    "OPTION": "Valgmulighed",
    "RESPONSES": "Stemmer",
    "SHARE": "Andel",
    "VIEW_POLL": "Se afstemning",
    "DISABLE_NOTIFICATIONS_FOR_POLL": "Deaktiver meddelelser for denne afstemning",
    "UNSUBSCRIBE_HERE": "Afmeld"
}