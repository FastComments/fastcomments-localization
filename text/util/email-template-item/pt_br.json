{
    "TEXT_CUSTOM_TEMPLATE_NAME": "<%= template.displayName ? template.displayName : 'Modelo Personalizado: ' + viewTemplate.name %>",
    "TEXT_RENDER_ERRORS": "<%= Number(template.errorCount).toLocaleString() %> <%= template.errorCount === 1 ? 'Erro de Renderização Recente!' : 'Erros de Renderização Recentes!' %>"
}
